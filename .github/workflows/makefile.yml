name: Makefile CI

on:
  push:
    branches: [ lzd-os ]
  pull_request:
    branches: [ lzd-os ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: configure
      run: make run
